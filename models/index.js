// Importing User model from './user'
const User = require('./user');

// Importing Thought model from './thought'
const Thought = require('./thought');

// Exporting an object containing User and Thought models
module.exports = { User, Thought };
